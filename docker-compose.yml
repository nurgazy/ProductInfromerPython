version: '3.8'

services:
  fastapi_app:
    build: .
    image: product_informer_img:latest
    container_name: product_informer
    ports:
      - "80:8000"
    volumes:
      - /home/ubuntu/uploads:/app/temp_files
    environment:
      - ENV=development
    restart: always